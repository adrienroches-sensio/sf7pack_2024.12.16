framework:
    http_client:
        scoped_clients:
            events.client:
                base_uri: 'https://www.devevents-api.fr'
                headers:
                    Accept: 'application/json'
                    apikey: '%env(DEVEVENTSAPI_KEY)%'
